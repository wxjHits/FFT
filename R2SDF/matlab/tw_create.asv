N=8;
QUANTI_BIT = 16 ;%16bit有符号数进行量化

kn = 0:1:N-1;

w_real = cos(-2*pi*kn/N);
w_img  = sin(-2*pi*kn/N);

w_real_d = round(w_real*2^(QUANTI_BIT-1));
w_img_d  = round(w_img*2^(QUANTI_BIT-1));

file = fopen('tw.txt','wt');
    for i = 0:N-1
        
        if(w_real_d<0)%负数取补码
            
        end
         ss = dec2hex(w_real_d,16);
         fprintf(file,'%s\n',ss);
    end
fclose(file);
